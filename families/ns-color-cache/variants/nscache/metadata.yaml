# LLM-assisted: Claude Code (claude-sonnet-4-6), 2026-06-25
id: ns-color-cache-nscache
family: ns-color-cache
title: "NSColor cache backed by NSCache with explicit countLimit (512)"
llm_assisted: true
llm_system: "Codex"
human_review_status: "not-reviewed"
base_emacs_revision: "unknown — see BASELINE.md; blob src/nsterm.m: a7f3fc292c"
files_modified:
  - src/nsterm.m
design_dimensions:
  cache_structure: "NSCache"
  count_limit: 512
  eviction_policy: "automatic under system memory pressure (advisory countLimit)"
  ownership: "NSCache retains values; caller's returned pointer valid at call time"
  thread_safety: "NSCache is thread-safe by API contract"
  scope: "global"
  initialization: "ns_nscache_init() called from ns_initialize_display_info"
  teardown: "ns_nscache_teardown() called from ns_term_shutdown"
dependencies: []
conflicts:
  - ns-color-cache-baseline
  - ns-color-cache-open-addressing
  - ns-color-cache-per-frame
  - ns-color-cache-counted-lru
  - ns-color-cache-single-entry-hot-cache
  - ns-color-cache-two-way-set-associative
  - ns-color-cache-nsmutable-dictionary
  - ns-color-cache-per-display-backend
expected_tradeoffs:
  benefits:
    - "Automatic eviction under memory pressure reduces RSS without explicit policy"
    - "Thread-safe by contract (no synchronization needed even if drawing path parallelizes)"
    - "No hand-written replacement algorithm"
    - "Caller-held pointer remains valid while in scope even if cache evicts the entry"
  costs:
    - "Non-deterministic eviction: cache size and evicted entries depend on system state"
    - "countLimit is advisory: NSCache may hold more or fewer than 512 entries"
    - "Heavier than flat C array: Objective-C messaging overhead per lookup"
    - "No production recommendation without benchmark evidence comparing to CLOCK-LRU"
normalized_patch_sha256: "not-computed"
apply_status: "design-space variant — not tested against Emacs source"
build_status: "not-tested"
test_status: "not-tested"
benchmark_status: "not-tested"
